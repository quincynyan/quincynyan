{
	"scripts": {
		"predeploy": "cd about-me && npm run build",
		"deploy": "cd about-me && gh-pages -d out",
		"server": "cd about-me && npm run dev",
		"push": "git push && cd about-me && npm run build && npm run export && npm run deploy"
	},
	"devDependencies": {
		"gh-pages": "5.0.0"
	},
	"homepage": "https://quinc.nekonyan.fun",
	"dependencies": {
		"twemoji": "14.0.2"
	}
}
