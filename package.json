{
	"scripts": {
		"predeploy": "cd about-me && npm run build",
		"deploy": "gh-pages -d out",
		"start": "cd about-me && npm run dev",
		"publish": "git push origin master && cd about-me && npm run build && npm run export && npm run deploy"
	},
	"devDependencies": {
		"gh-pages": "5.0.0"
	},
	"homepage": "https://quinc.nekonyan.fun",
	"dependencies": {
		"twemoji": "14.0.2"
	}
}
